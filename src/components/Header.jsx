import React from "react";
import { AppBar, Container, Toolbar } from "@mui/material";

export function Header() {
  return (
    <AppBar>
      <Container>
        <Toolbar />
      </Container>
    </AppBar>
  );
}
